<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yxb.video.mapper.VideoSubtitleMapper">

    <select id="selectByVideoId" resultType="com.yxb.video.domain.entity.VideoSubtitle">
        SELECT * FROM video_subtitle WHERE video_id = #{videoId} AND deleted = 0 ORDER BY id
    </select>

    <select id="selectByVideoIdAndLanguage" resultType="com.yxb.video.domain.entity.VideoSubtitle">
        SELECT * FROM video_subtitle WHERE video_id = #{videoId} AND language = #{language} AND deleted = 0 LIMIT 1
    </select>
</mapper>
